#include<stdio.h>

int main(void){
    int num1=0, num2=0, num3=0;
    int mul=0;
    string mul_str =''
    scanf("%d", &num1);
    scanf("%d", &num2);
    scanf("%d", &num3);
    mul = num1*num2*num3;
    mul_str=itoa(mul, len(mul), 10);
    
}
